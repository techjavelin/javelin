# Environment variable examples for local development
# Copy this file to `.env.local` (gitignored) and adjust values.

# Base URL for custom Admin API (API Gateway invoke URL, no trailing slash)
# Example: https://abc123.execute-api.us-east-1.amazonaws.com/prod
VITE_ADMIN_API_BASE=

# Amplify / Vite can also load from .env, .env.local etc. See Vite docs for precedence.
